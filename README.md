## apollo_f767ig for rust

### build
```bash
cargo led # only for build led binary
```

### flash(should install cargo-flash)
```bash
cargo install cargo-flash
```

```bash
cargo fl-767 --bin led
```
